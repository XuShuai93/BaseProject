apply from: rootProject.file("gradle/library.gradle")

android {
    defaultConfig {
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation 'com.tencent:mmkv:1.2.13'
}